<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CuisineQuest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CuisineQuest" parent="Base.Theme.CuisineQuest" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/Blue</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/appiconv3</item>
        <item name="postSplashScreenTheme">@style/Theme.CuisineQuest</item>

    </style>

</resources>